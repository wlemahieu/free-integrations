##### Getting Started
* Run `yarn install`
* Copy `.env-model` to `.env` & and update with real API keys.
* Ensure `./client/README` instructions were followed.
##### Start the app
* Run `yarn run dev`
